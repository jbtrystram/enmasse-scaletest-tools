apiVersion: v1
kind: Service
metadata:
  labels:
    app: enmasse
    component: iot
    name: iot-device-registry-debug-endpoint
  name: iot-device-registry-debug-endpoint
spec:
  ports:
  - name: http
    port: 80
    targetPort: 44120
    protocol: TCP
  selector:
    app: enmasse
    component: iot
    name: iot-device-registry
